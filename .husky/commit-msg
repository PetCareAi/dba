#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "📝 Verificando mensagem do commit..."

# Ler a mensagem do commit
mensagem_commit=$(cat "$1")

# Remover linhas de comentário (que começam com #)
mensagem_limpa=$(echo "$mensagem_commit" | grep -v "^#" | sed '/^\s*$/d')

# Verificar se a mensagem não está vazia
if [ -z "$mensagem_limpa" ]; then
    echo "❌ Mensagem do commit está vazia"
    echo "   Forneça uma mensagem descritiva para o commit"
    exit 1
fi

# Verificar tamanho mínimo da mensagem
tamanho_minimo=10
if [ ${#mensagem_limpa} -lt $tamanho_minimo ]; then
    echo "❌ Mensagem do commit muito curta (mínimo: $tamanho_minimo caracteres)"
    echo "   Mensagem atual: '$mensagem_limpa'"
    echo "   Forneça uma descrição mais detalhada das mudanças"
    exit 1
fi

# Verificar primeira linha (título do commit)
primeira_linha=$(echo "$mensagem_limpa" | head -n1)

# Verificar tamanho da primeira linha (título)
tamanho_titulo=${#primeira_linha}
if [ $tamanho_titulo -gt 72 ]; then
    echo "⚠️ Título do commit muito longo ($tamanho_titulo chars, máx: 72)"
    echo "   Título: '$primeira_linha'"
    echo "   Considere encurtar o título e usar o corpo para detalhes"
fi

# Verificar se o título não termina com ponto
if echo "$primeira_linha" | grep -q "\.$"; then
    echo "⚠️ Título do commit não deve terminar com ponto"
    echo "   Título: '$primeira_linha'"
fi

# Verificar padrões recomendados (conventional commits)
if echo "$primeira_linha" | grep -qE "^(feat|fix|docs|style|refactor|test|chore|perf|ci|build|revert)(\(.+\))?:"; then
    echo "✅ Formato Conventional Commits detectado"
else
    echo "💡 Dica: Considere usar Conventional Commits"
    echo "   Exemplos: feat: adicionar funcionalidade X"
    echo "            fix: corrigir bug na conexão"
    echo "            docs: atualizar README"
fi

# Verificar padrões a evitar
if echo "$mensagem_limpa" | grep -qiE "^(wip|test|temp|debug|fix|update|change)$"; then
    echo "⚠️ Mensagem muito genérica detectada"
    echo "   Mensagem: '$mensagem_limpa'"
    echo "   Seja mais específico sobre o que foi alterado"
    echo "   Exemplos bons:"
    echo "   - feat(auth): implementar autenticação com Supabase"
    echo "   - fix(db): corrigir timeout na conexão"
    echo "   - docs: adicionar documentação da API"
fi

# Verificar palavras proibidas ou problemáticas
if echo "$mensagem_limpa" | grep -qiE "(fuck|shit|damn|crap|stupid|bad|hack)"; then
    echo "❌ Linguagem inadequada detectada na mensagem"
    echo "   Use linguagem profissional nos commits"
    exit 1
fi

# Verificar se há referência a issues/tickets
if echo "$mensagem_limpa" | grep -qE "(#[0-9]+|issue|ticket|bug|close|fix|resolve)"; then
    echo "✅ Referência a issue/ticket detectada"
fi

# Verificar múltiplas linhas (boa prática)
linhas_count=$(echo "$mensagem_limpa" | wc -l)
if [ $linhas_count -gt 1 ]; then
    echo "✅ Commit com corpo/descrição detalhada"
    
    # Verificar linha em branco após título
    segunda_linha=$(echo "$mensagem_limpa" | sed -n '2p')
    if [ ! -z "$segunda_linha" ]; then
        echo "⚠️ Deve haver uma linha em branco após o título"
        echo "   Formato recomendado:"
        echo "   título do commit"
        echo "   "
        echo "   descrição detalhada..."
    fi
fi

# Verificar por marcadores comuns de trabalho em progresso
if echo "$mensagem_limpa" | grep -qiE "(wip|todo|fixme|xxx|hack|temporary)"; then
    echo "⚠️ Marcadores de trabalho em progresso detectados"
    echo "   Considere finalizar o trabalho antes do commit"
    read -p "Continuar mesmo assim? (s/N): " confirmacao
    if [ "$confirmacao" != "s" ] && [ "$confirmacao" != "S" ]; then
        echo "❌ Commit cancelado pelo usuário"
        exit 1
    fi
fi

# Estatísticas da mensagem
echo "📊 Estatísticas da mensagem:"
echo "   📏 Tamanho total: ${#mensagem_limpa} caracteres"
echo "   📄 Linhas: $linhas_count"
echo "   📝 Título: '$primeira_linha' (${#primeira_linha} chars)"

# Verificação de categoria automática
categoria="geral"
if echo "$mensagem_limpa" | grep -qiE "(feat|feature|add|novo|implementar)"; then
    categoria="funcionalidade"
elif echo "$mensagem_limpa" | grep -qiE "(fix|bug|corrig|erro|problema)"; then
    categoria="correção"
elif echo "$mensagem_limpa" | grep -qiE "(docs|documentação|readme)"; then
    categoria="documentação"
elif echo "$mensagem_limpa" | grep -qiE "(style|format|lint|clean)"; then
    categoria="estilo"
elif echo "$mensagem_limpa" | grep -qiE "(refactor|reorganiz|reestrutur)"; then
    categoria="refatoração"
elif echo "$mensagem_limpa" | grep -qiE "(test|teste)"; then
    categoria="teste"
elif echo "$mensagem_limpa" | grep -qiE "(perf|performance|otimiz)"; then
    categoria="performance"
fi

echo "   📂 Categoria detectada: $categoria"

echo "✅ Mensagem do commit aprovada!"
